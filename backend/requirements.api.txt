# ====================================================
# API-ONLY DEPENDENCIES
# Backend FastAPI server (no ML models)
# ====================================================

-r requirements.common.txt

# neckenml Core - MIT licensed (no AGPL audio dependencies)
neckenml-core>=0.3.0

# Web Server
uvicorn[standard]         # ASGI server for FastAPI
