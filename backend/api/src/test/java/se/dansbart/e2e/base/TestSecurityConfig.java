package se.dansbart.e2e.base;

import org.springframework.boot.test.context.TestConfiguration;

/**
 * Placeholder — no longer needed now that auth is DiscourseConnect session-based
 * and tests use UsernamePasswordAuthenticationToken directly via JwtTestHelper.
 */
@TestConfiguration
public class TestSecurityConfig {
}
