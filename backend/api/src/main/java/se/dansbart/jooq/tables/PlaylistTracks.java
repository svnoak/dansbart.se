/*
 * This file is generated by jOOQ.
 */
package se.dansbart.jooq.tables;


import java.time.LocalDateTime;
import java.util.Arrays;
import java.util.Collection;
import java.util.List;
import java.util.UUID;

import org.jooq.Condition;
import org.jooq.Field;
import org.jooq.ForeignKey;
import org.jooq.Index;
import org.jooq.InverseForeignKey;
import org.jooq.Name;
import org.jooq.Path;
import org.jooq.PlainSQL;
import org.jooq.QueryPart;
import org.jooq.Record;
import org.jooq.SQL;
import org.jooq.Schema;
import org.jooq.Select;
import org.jooq.Stringly;
import org.jooq.Table;
import org.jooq.TableField;
import org.jooq.TableOptions;
import org.jooq.UniqueKey;
import org.jooq.impl.DSL;
import org.jooq.impl.SQLDataType;
import org.jooq.impl.TableImpl;

import se.dansbart.jooq.Indexes;
import se.dansbart.jooq.Keys;
import se.dansbart.jooq.Public;
import se.dansbart.jooq.tables.Playlists.PlaylistsPath;
import se.dansbart.jooq.tables.Tracks.TracksPath;


/**
 * This class is generated by jOOQ.
 */
@SuppressWarnings({ "all", "unchecked", "rawtypes", "this-escape" })
public class PlaylistTracks extends TableImpl<Record> {

    private static final long serialVersionUID = 1L;

    /**
     * The reference instance of <code>public.playlist_tracks</code>
     */
    public static final PlaylistTracks PLAYLIST_TRACKS = new PlaylistTracks();

    /**
     * The class holding records for this type
     */
    @Override
    public Class<Record> getRecordType() {
        return Record.class;
    }

    /**
     * The column <code>public.playlist_tracks.id</code>.
     */
    public final TableField<Record, UUID> ID = createField(DSL.name("id"), SQLDataType.UUID.nullable(false), this, "");

    /**
     * The column <code>public.playlist_tracks.playlist_id</code>.
     */
    public final TableField<Record, UUID> PLAYLIST_ID = createField(DSL.name("playlist_id"), SQLDataType.UUID.nullable(false), this, "");

    /**
     * The column <code>public.playlist_tracks.track_id</code>.
     */
    public final TableField<Record, UUID> TRACK_ID = createField(DSL.name("track_id"), SQLDataType.UUID.nullable(false), this, "");

    /**
     * The column <code>public.playlist_tracks.position</code>.
     */
    public final TableField<Record, Integer> POSITION = createField(DSL.name("position"), SQLDataType.INTEGER.nullable(false), this, "");

    /**
     * The column <code>public.playlist_tracks.added_at</code>.
     */
    public final TableField<Record, LocalDateTime> ADDED_AT = createField(DSL.name("added_at"), SQLDataType.LOCALDATETIME(6).nullable(false).defaultValue(DSL.field(DSL.raw("now()"), SQLDataType.LOCALDATETIME)), this, "");

    private PlaylistTracks(Name alias, Table<Record> aliased) {
        this(alias, aliased, (Field<?>[]) null, null);
    }

    private PlaylistTracks(Name alias, Table<Record> aliased, Field<?>[] parameters, Condition where) {
        super(alias, null, aliased, parameters, DSL.comment(""), TableOptions.table(), where);
    }

    /**
     * Create an aliased <code>public.playlist_tracks</code> table reference
     */
    public PlaylistTracks(String alias) {
        this(DSL.name(alias), PLAYLIST_TRACKS);
    }

    /**
     * Create an aliased <code>public.playlist_tracks</code> table reference
     */
    public PlaylistTracks(Name alias) {
        this(alias, PLAYLIST_TRACKS);
    }

    /**
     * Create a <code>public.playlist_tracks</code> table reference
     */
    public PlaylistTracks() {
        this(DSL.name("playlist_tracks"), null);
    }

    public <O extends Record> PlaylistTracks(Table<O> path, ForeignKey<O, Record> childPath, InverseForeignKey<O, Record> parentPath) {
        super(path, childPath, parentPath, PLAYLIST_TRACKS);
    }

    /**
     * A subtype implementing {@link Path} for simplified path-based joins.
     */
    public static class PlaylistTracksPath extends PlaylistTracks implements Path<Record> {

        private static final long serialVersionUID = 1L;
        public <O extends Record> PlaylistTracksPath(Table<O> path, ForeignKey<O, Record> childPath, InverseForeignKey<O, Record> parentPath) {
            super(path, childPath, parentPath);
        }
        private PlaylistTracksPath(Name alias, Table<Record> aliased) {
            super(alias, aliased);
        }

        @Override
        public PlaylistTracksPath as(String alias) {
            return new PlaylistTracksPath(DSL.name(alias), this);
        }

        @Override
        public PlaylistTracksPath as(Name alias) {
            return new PlaylistTracksPath(alias, this);
        }

        @Override
        public PlaylistTracksPath as(Table<?> alias) {
            return new PlaylistTracksPath(alias.getQualifiedName(), this);
        }
    }

    @Override
    public Schema getSchema() {
        return aliased() ? null : Public.PUBLIC;
    }

    @Override
    public List<Index> getIndexes() {
        return Arrays.asList(Indexes.IX_PLAYLIST_TRACKS_PLAYLIST_ID, Indexes.IX_PLAYLIST_TRACKS_TRACK_ID);
    }

    @Override
    public UniqueKey<Record> getPrimaryKey() {
        return Keys.PLAYLIST_TRACKS_PKEY;
    }

    @Override
    public List<UniqueKey<Record>> getUniqueKeys() {
        return Arrays.asList(Keys.UQ_PLAYLIST_TRACK);
    }

    @Override
    public List<ForeignKey<Record, ?>> getReferences() {
        return Arrays.asList(Keys.PLAYLIST_TRACKS__PLAYLIST_TRACKS_PLAYLIST_ID_FKEY, Keys.PLAYLIST_TRACKS__PLAYLIST_TRACKS_TRACK_ID_FKEY);
    }

    private transient PlaylistsPath _playlists;

    /**
     * Get the implicit join path to the <code>public.playlists</code> table.
     */
    public PlaylistsPath playlists() {
        if (_playlists == null)
            _playlists = new PlaylistsPath(this, Keys.PLAYLIST_TRACKS__PLAYLIST_TRACKS_PLAYLIST_ID_FKEY, null);

        return _playlists;
    }

    private transient TracksPath _tracks;

    /**
     * Get the implicit join path to the <code>public.tracks</code> table.
     */
    public TracksPath tracks() {
        if (_tracks == null)
            _tracks = new TracksPath(this, Keys.PLAYLIST_TRACKS__PLAYLIST_TRACKS_TRACK_ID_FKEY, null);

        return _tracks;
    }

    @Override
    public PlaylistTracks as(String alias) {
        return new PlaylistTracks(DSL.name(alias), this);
    }

    @Override
    public PlaylistTracks as(Name alias) {
        return new PlaylistTracks(alias, this);
    }

    @Override
    public PlaylistTracks as(Table<?> alias) {
        return new PlaylistTracks(alias.getQualifiedName(), this);
    }

    /**
     * Rename this table
     */
    @Override
    public PlaylistTracks rename(String name) {
        return new PlaylistTracks(DSL.name(name), null);
    }

    /**
     * Rename this table
     */
    @Override
    public PlaylistTracks rename(Name name) {
        return new PlaylistTracks(name, null);
    }

    /**
     * Rename this table
     */
    @Override
    public PlaylistTracks rename(Table<?> name) {
        return new PlaylistTracks(name.getQualifiedName(), null);
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public PlaylistTracks where(Condition condition) {
        return new PlaylistTracks(getQualifiedName(), aliased() ? this : null, null, condition);
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public PlaylistTracks where(Collection<? extends Condition> conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public PlaylistTracks where(Condition... conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public PlaylistTracks where(Field<Boolean> condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public PlaylistTracks where(SQL condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public PlaylistTracks where(@Stringly.SQL String condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public PlaylistTracks where(@Stringly.SQL String condition, Object... binds) {
        return where(DSL.condition(condition, binds));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public PlaylistTracks where(@Stringly.SQL String condition, QueryPart... parts) {
        return where(DSL.condition(condition, parts));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public PlaylistTracks whereExists(Select<?> select) {
        return where(DSL.exists(select));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public PlaylistTracks whereNotExists(Select<?> select) {
        return where(DSL.notExists(select));
    }
}
