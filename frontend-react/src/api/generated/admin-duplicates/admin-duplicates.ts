/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * OpenAPI definition
 * OpenAPI spec version: v0
 */
import type {
  AnalyzeDuplicates200,
  GetDuplicates200,
  GetDuplicatesParams,
  GetMergeableDuplicates200,
  GetMergeableDuplicatesParams,
  MergeAllDuplicates200,
  MergeAllDuplicatesParams,
  MergeDuplicates200,
  MergeDuplicatesParams
} from '../../models';


/**
 * @summary Merge all duplicate tracks with the given ISRC into a single canonical track
 */
export const getMergeDuplicatesUrl = (isrc: string,
    params?: MergeDuplicatesParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/admin/tracks/duplicates/merge/${isrc}?${stringifiedParams}` : `/api/admin/tracks/duplicates/merge/${isrc}`
}

export const mergeDuplicates = async (isrc: string,
    params?: MergeDuplicatesParams, options?: RequestInit): Promise<MergeDuplicates200> => {
  
  const res = await fetch(getMergeDuplicatesUrl(isrc,params),
  {      
    ...options,
    method: 'POST'
    
    
  }
)

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  
  const data: MergeDuplicates200 = body ? JSON.parse(body) : {}
  return data
}


/**
 * @summary Merge ALL duplicate tracks in the database
 */
export const getMergeAllDuplicatesUrl = (params?: MergeAllDuplicatesParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/admin/tracks/duplicates/merge-all?${stringifiedParams}` : `/api/admin/tracks/duplicates/merge-all`
}

export const mergeAllDuplicates = async (params?: MergeAllDuplicatesParams, options?: RequestInit): Promise<MergeAllDuplicates200> => {
  
  const res = await fetch(getMergeAllDuplicatesUrl(params),
  {      
    ...options,
    method: 'POST'
    
    
  }
)

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  
  const data: MergeAllDuplicates200 = body ? JSON.parse(body) : {}
  return data
}


/**
 * @summary Get tracks that share the same ISRC (duplicates)
 */
export const getGetDuplicatesUrl = (params?: GetDuplicatesParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/admin/tracks/duplicates?${stringifiedParams}` : `/api/admin/tracks/duplicates`
}

export const getDuplicates = async (params?: GetDuplicatesParams, options?: RequestInit): Promise<GetDuplicates200> => {
  
  const res = await fetch(getGetDuplicatesUrl(params),
  {      
    ...options,
    method: 'GET'
    
    
  }
)

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  
  const data: GetDuplicates200 = body ? JSON.parse(body) : {}
  return data
}


/**
 * @summary Get list of ISRCs with duplicate tracks that can be merged
 */
export const getGetMergeableDuplicatesUrl = (params?: GetMergeableDuplicatesParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/admin/tracks/duplicates/mergeable?${stringifiedParams}` : `/api/admin/tracks/duplicates/mergeable`
}

export const getMergeableDuplicates = async (params?: GetMergeableDuplicatesParams, options?: RequestInit): Promise<GetMergeableDuplicates200> => {
  
  const res = await fetch(getGetMergeableDuplicatesUrl(params),
  {      
    ...options,
    method: 'GET'
    
    
  }
)

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  
  const data: GetMergeableDuplicates200 = body ? JSON.parse(body) : {}
  return data
}


/**
 * @summary Analyze duplicates for a specific ISRC
 */
export const getAnalyzeDuplicatesUrl = (isrc: string,) => {


  

  return `/api/admin/tracks/duplicates/analyze/${isrc}`
}

export const analyzeDuplicates = async (isrc: string, options?: RequestInit): Promise<AnalyzeDuplicates200> => {
  
  const res = await fetch(getAnalyzeDuplicatesUrl(isrc),
  {      
    ...options,
    method: 'GET'
    
    
  }
)

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  
  const data: AnalyzeDuplicates200 = body ? JSON.parse(body) : {}
  return data
}


