/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * OpenAPI definition
 * OpenAPI spec version: v0
 */
import type {
  ConfirmSecondaryStyle200,
  FeedbackRequest,
  FlagTrack200,
  FlagTrackParams,
  GetSimilarTracksParams,
  GetTracksParams,
  MovementVoteRequest,
  PageResponseTrackListDto,
  PlaybackLink,
  SearchTracksParams,
  SecondaryStyleRequest,
  StructureRequest,
  SubmitLinkRequest,
  SubmitMovementVote200,
  SubmitStructureProposal200,
  SubmitStructureProposalParams,
  Track,
  TrackStructureVersion,
  TrackStyleVote,
  UnflagTrack200
} from '../../models';


/**
 * @summary Submit a structure proposal (creates candidate version)
 */
export const getSubmitStructureProposalUrl = (id: string,
    params?: SubmitStructureProposalParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/tracks/${id}/structure?${stringifiedParams}` : `/api/tracks/${id}/structure`
}

export const submitStructureProposal = async (id: string,
    structureRequest: StructureRequest,
    params?: SubmitStructureProposalParams, options?: RequestInit): Promise<SubmitStructureProposal200> => {
  
  const res = await fetch(getSubmitStructureProposalUrl(id,params),
  {      
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      structureRequest,)
  }
)

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  
  const data: SubmitStructureProposal200 = body ? JSON.parse(body) : {}
  return data
}


/**
 * @summary Submit movement tags for a track
 */
export const getSubmitMovementVoteUrl = (id: string,) => {


  

  return `/api/tracks/${id}/movement`
}

export const submitMovementVote = async (id: string,
    movementVoteRequest: MovementVoteRequest, options?: RequestInit): Promise<SubmitMovementVote200> => {
  
  const res = await fetch(getSubmitMovementVoteUrl(id),
  {      
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      movementVoteRequest,)
  }
)

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  
  const data: SubmitMovementVote200 = body ? JSON.parse(body) : {}
  return data
}


/**
 * @summary Submit a new playback link for a track
 */
export const getSubmitLinkUrl = (id: string,) => {


  

  return `/api/tracks/${id}/links`
}

export const submitLink = async (id: string,
    submitLinkRequest: SubmitLinkRequest, options?: RequestInit): Promise<PlaybackLink> => {
  
  const res = await fetch(getSubmitLinkUrl(id),
  {      
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      submitLinkRequest,)
  }
)

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  
  const data: PlaybackLink = body ? JSON.parse(body) : {}
  return data
}


/**
 * @summary Flag a track as not being folk music
 */
export const getFlagTrackUrl = (id: string,
    params?: FlagTrackParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/tracks/${id}/flag?${stringifiedParams}` : `/api/tracks/${id}/flag`
}

export const flagTrack = async (id: string,
    params?: FlagTrackParams, options?: RequestInit): Promise<FlagTrack200> => {
  
  const res = await fetch(getFlagTrackUrl(id,params),
  {      
    ...options,
    method: 'POST'
    
    
  }
)

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  
  const data: FlagTrack200 = body ? JSON.parse(body) : {}
  return data
}


/**
 * @summary Remove flag from a track (admin override)
 */
export const getUnflagTrackUrl = (id: string,) => {


  

  return `/api/tracks/${id}/flag`
}

export const unflagTrack = async (id: string, options?: RequestInit): Promise<UnflagTrack200> => {
  
  const res = await fetch(getUnflagTrackUrl(id),
  {      
    ...options,
    method: 'DELETE'
    
    
  }
)

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  
  const data: UnflagTrack200 = body ? JSON.parse(body) : {}
  return data
}


/**
 * @summary Submit style correction feedback for a track
 */
export const getSubmitFeedbackUrl = (id: string,) => {


  

  return `/api/tracks/${id}/feedback`
}

export const submitFeedback = async (id: string,
    feedbackRequest: FeedbackRequest, options?: RequestInit): Promise<TrackStyleVote> => {
  
  const res = await fetch(getSubmitFeedbackUrl(id),
  {      
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      feedbackRequest,)
  }
)

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  
  const data: TrackStyleVote = body ? JSON.parse(body) : {}
  return data
}


/**
 * @summary Confirm a secondary dance style without affecting primary election
 */
export const getConfirmSecondaryStyleUrl = (id: string,) => {


  

  return `/api/tracks/${id}/confirm-secondary`
}

export const confirmSecondaryStyle = async (id: string,
    secondaryStyleRequest: SecondaryStyleRequest, options?: RequestInit): Promise<ConfirmSecondaryStyle200> => {
  
  const res = await fetch(getConfirmSecondaryStyleUrl(id),
  {      
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      secondaryStyleRequest,)
  }
)

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  
  const data: ConfirmSecondaryStyle200 = body ? JSON.parse(body) : {}
  return data
}


/**
 * @summary Report a broken playback link
 */
export const getReportBrokenLinkUrl = (linkId: string,) => {


  

  return `/api/tracks/links/${linkId}/report`
}

export const reportBrokenLink = async (linkId: string, options?: RequestInit): Promise<void> => {
  
  const res = await fetch(getReportBrokenLinkUrl(linkId),
  {      
    ...options,
    method: 'PATCH'
    
    
  }
)

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  
  const data: void = body ? JSON.parse(body) : {}
  return data
}


/**
 * @summary Get playable tracks with optional filters
 */
export const getGetTracksUrl = (params?: GetTracksParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/tracks?${stringifiedParams}` : `/api/tracks`
}

export const getTracks = async (params?: GetTracksParams, options?: RequestInit): Promise<PageResponseTrackListDto> => {
  
  const res = await fetch(getGetTracksUrl(params),
  {      
    ...options,
    method: 'GET'
    
    
  }
)

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  
  const data: PageResponseTrackListDto = body ? JSON.parse(body) : {}
  return data
}


/**
 * @summary Get track by ID
 */
export const getGetTrackUrl = (id: string,) => {


  

  return `/api/tracks/${id}`
}

export const getTrack = async (id: string, options?: RequestInit): Promise<Track> => {
  
  const res = await fetch(getGetTrackUrl(id),
  {      
    ...options,
    method: 'GET'
    
    
  }
)

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  
  const data: Track = body ? JSON.parse(body) : {}
  return data
}


/**
 * @summary Get list of structure versions for a track
 */
export const getGetStructureVersionsUrl = (id: string,) => {


  

  return `/api/tracks/${id}/structure-versions`
}

export const getStructureVersions = async (id: string, options?: RequestInit): Promise<TrackStructureVersion[]> => {
  
  const res = await fetch(getGetStructureVersionsUrl(id),
  {      
    ...options,
    method: 'GET'
    
    
  }
)

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  
  const data: TrackStructureVersion[] = body ? JSON.parse(body) : {}
  return data
}


/**
 * @summary Get similar tracks using embedding similarity
 */
export const getGetSimilarTracksUrl = (id: string,
    params?: GetSimilarTracksParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/tracks/${id}/similar?${stringifiedParams}` : `/api/tracks/${id}/similar`
}

export const getSimilarTracks = async (id: string,
    params?: GetSimilarTracksParams, options?: RequestInit): Promise<Track[]> => {
  
  const res = await fetch(getGetSimilarTracksUrl(id,params),
  {      
    ...options,
    method: 'GET'
    
    
  }
)

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  
  const data: Track[] = body ? JSON.parse(body) : {}
  return data
}


/**
 * @summary Search tracks by title
 */
export const getSearchTracksUrl = (params: SearchTracksParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/tracks/search?${stringifiedParams}` : `/api/tracks/search`
}

export const searchTracks = async (params: SearchTracksParams, options?: RequestInit): Promise<PageResponseTrackListDto> => {
  
  const res = await fetch(getSearchTracksUrl(params),
  {      
    ...options,
    method: 'GET'
    
    
  }
)

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  
  const data: PageResponseTrackListDto = body ? JSON.parse(body) : {}
  return data
}


